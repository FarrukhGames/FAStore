import {Link} from 'react-router-dom';
const Navigation = () => {
    return (
        <nav className="navigation">
            <a href="#">Меню</a>
            <a href="#">Корзина</a>
        </nav>
    );
}

export default Navigation;